<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.dangerous" default="build" basedir=".">
    <property name="deb.dangerous.setup" value="${deb.setup}/dangerous"/>
    <property name="deb.dangerous.include" value="${basedir}/include"/>
    <property name="deb.dangerous.bin" value="${basedir}/dangerous"/>
    <property name="deb.dangerous.staging" value="${deb.staging}/dangerous"/>

    <property name="deb.dangerous.outputfile" value="${deb.builddir}/HUGnetScripts_dangerous_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.dangerous.staging}" failonerror="false"/>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetScripts 'v${deb.version}'</echo>

        <mkdir dir="${deb.dangerous.staging}"/>
        <copy todir="${deb.dangerous.staging}">
            <fileset dir="${deb.dangerous.setup}"/>
        </copy>
        <mkdir dir="${deb.dangerous.staging}${deb.prefix}/bin"/>
        <copy todir="${deb.dangerous.staging}${deb.prefix}/bin">
            <fileset dir="${deb.dangerous.bin}"/>
        </copy>
        <chmod dir="${deb.dangerous.staging}${deb.prefix}/bin" perm="ugo+rx"
               includes="*"/>
        <chmod file="${deb.dangerous.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.dangerous.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <replaceregexp file="${deb.dangerous.staging}/DEBIAN/control" flags="g"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.dangerous.staging}
                       ${deb.dangerous.outputfile}"/>
        </exec>
    </target>
    <target name="sign" description="sign the deb">
        <exec executable="dpkg-sig" failonerror="true" dir="${deb.builddir}">
            <arg line="-s builder
                       ${deb.dangerous.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.dangerous.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.dangerous.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.dangerous.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.dangerous.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
