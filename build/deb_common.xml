<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.common" default="build" basedir=".">
    <property name="deb.common.setup" value="${deb.setup}/common"/>
    <property name="deb.common.include" value="${basedir}/include"/>
    <property name="deb.common.bin" value="${basedir}/bin"/>
    <property name="deb.common.staging" value="${deb.staging}/common"/>

    <property name="deb.common.outputfile" value="${deb.builddir}/HUGnetScripts_common_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.common.staging}" failonerror="false"/>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetScripts 'v${deb.version}'</echo>

        <mkdir dir="${deb.common.staging}"/>
        <copy todir="${deb.common.staging}">
            <fileset dir="${deb.common.setup}"/>
        </copy>
        <mkdir dir="${deb.common.staging}/usr/share/php/HUGnetScripts"/>
        <copy todir="${deb.common.staging}/usr/share/php/HUGnetScripts">
            <fileset dir="${deb.common.include}"/>
        </copy>
        <mkdir dir="${deb.common.staging}${deb.prefix}/bin"/>
        <copy todir="${deb.common.staging}${deb.prefix}/bin">
            <fileset dir="${deb.common.bin}"/>
        </copy>
        <chmod dir="${deb.common.staging}${deb.prefix}/bin" perm="ugo+rx"
               includes="*"/>
        <chmod file="${deb.common.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.common.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <replaceregexp file="${deb.common.staging}/DEBIAN/control" byline="yes"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.common.staging}
                       ${deb.common.outputfile}"/>
        </exec>
    </target>
    <target name="sign" description="sign the deb">
        <exec executable="dpkg-sig" failonerror="true" dir="${deb.builddir}">
            <arg line="-s builder
                       ${deb.common.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.common.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.common.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.common.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.common.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
