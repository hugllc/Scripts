<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.watchdog" default="build" basedir=".">
    <property name="deb.watchdog.setup" value="${deb.setup}/watchdog"/>
    <property name="deb.watchdog.include" value="${basedir}/include"/>
    <property name="deb.watchdog.bin" value="${basedir}/bin"/>
    <property name="deb.watchdog.staging" value="${deb.staging}/watchdog"/>

    <property name="deb.watchdog.outputfile" value="${deb.builddir}/HUGnetScripts_watchdog_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.watchdog.staging}" failonerror="false"/>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetScripts 'v${deb.version}'</echo>

        <mkdir dir="${deb.watchdog.staging}"/>
        <copy todir="${deb.watchdog.staging}">
            <fileset dir="${deb.watchdog.setup}"/>
        </copy>
        <chmod file="${deb.watchdog.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.watchdog.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <replaceregexp file="${deb.watchdog.staging}/DEBIAN/control" flags="g"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.watchdog.staging}
                       ${deb.watchdog.outputfile}"/>
        </exec>
    </target>
    <target name="sign" description="sign the deb">
        <exec executable="dpkg-sig" failonerror="true" dir="${deb.builddir}">
            <arg line="-s builder
                       ${deb.watchdog.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.watchdog.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.watchdog.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.watchdog.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.watchdog.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
