<?xml version="1.0" encoding="UTF-8"?>
<!-- Generated by PHP Project Wizard (PPW) 1.0.4 on Mon Oct 3 15:21:39 CDT 2011 -->

<project name="deb.misc" default="build" basedir=".">
    <property name="deb.misc.setup" value="${deb.setup}/misc"/>
    <property name="deb.misc.include" value="${basedir}/include"/>
    <property name="deb.misc.bin" value="${basedir}/misc"/>
    <property name="deb.misc.staging" value="${deb.staging}/misc"/>

    <property name="deb.misc.outputfile" value="${deb.builddir}/HUGnetScripts_misc_${deb.version}${deb.release}_all.deb" />

    <target name="clean" description="Clean up and create artifact directories">
        <delete dir="${deb.misc.staging}" failonerror="false"/>
    </target>

    <target name="setup" description="Copy all of the files to build the deb">
        <echo>Setting up for HUGnetScripts 'v${deb.version}'</echo>

        <mkdir dir="${deb.misc.staging}"/>
        <copy todir="${deb.misc.staging}">
            <fileset dir="${deb.misc.setup}"/>
        </copy>
        <mkdir dir="${deb.misc.staging}${deb.prefix}/bin"/>
        <copy todir="${deb.misc.staging}${deb.prefix}/bin">
            <fileset dir="${deb.misc.bin}"/>
        </copy>
        <chmod dir="${deb.misc.staging}${deb.prefix}/bin" perm="ugo+rx"
               includes="*"/>
        <chmod file="${deb.misc.staging}/DEBIAN/postinst" perm="ugo+rx"/>
        <chmod file="${deb.misc.staging}/DEBIAN/prerm" perm="ugo+rx"/>
        <replaceregexp file="${deb.misc.staging}/DEBIAN/control" flags="g"
                    match="%VERSION%"
                    replace="${deb.version}"
        />
    </target>
    <target name="deb" description="build the deb">
        <mkdir dir="${deb.builddir}"/>
        <exec executable="dpkg" failonerror="true">
            <arg line="-b ${deb.misc.staging}
                       ${deb.misc.outputfile}"/>
        </exec>
    </target>
    <target name="sign" description="sign the deb">
        <exec executable="dpkg-sig" failonerror="true" dir="${deb.builddir}">
            <arg line="-s builder
                       ${deb.misc.outputfile}"/>
        </exec>
    </target>
    <target name="post" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.misc.outputfile}
                        ${deb.postloc}main/"/>
        </exec>
    </target>
    <target name="testpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.misc.outputfile}
                        ${deb.postloc}testing/"/>
        </exec>
    </target>
    <target name="rcpost" description="post the deb" depends="build">
        <exec executable="rsync" failonerror="true" dir="${deb.builddir}">
            <arg line="-av ${deb.misc.outputfile}
                        ${deb.postloc}rc/"/>
        </exec>
    </target>
    <target name="postclean" description="build the deb">
        <delete dir="${deb.misc.staging}"/>
    </target>
    <target name="build" depends="clean,setup,deb"/>

</project>
